<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>{{ page.title }}</title>
        <link rel="stylesheet" href="style.css" />
    </head>
    <body>
    <div class="container">
        {% include "header.liquid" %}
        <div>
            {{ page.content }}
        </div>
        {% include "footer.liquid" %}
    </div>
  </body>
</html>
